Json ? => javaScrpit object Notation (자바스크립트 객체 표기식)

1_2, 1_test에서 ,로 구분하는 경우 문제가 발생한다. 데이터 형식은 그냥 json이라는 것을 쓰면 더욱 편리하기 때문에 json을 배워보자
데이터를 배열 또는 객체로 다양하게 표현 할 수 있다. 어떤 언어를 쓰던, 문자열을 사용해서 다른 프로그램과 통신해야 할 경우 json을 반드시 사용한다.

ex)
{
  "color" : "red",
  "message" : "안녕하세요", 
}

#규정
1. 키는 문자열만 사용할수 있다.
2. 문자열은 " "으로만 만들어야 한다.
3. Json.stringify() : 자바스크립트 객체 -> json 문자열
4. JSON.parse() : json문자열 -> 자바스크립트 객체